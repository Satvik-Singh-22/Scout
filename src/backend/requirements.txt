# Banquoite Backend — Python Dependencies
# Install: pip install -r requirements.txt

# Web Framework
fastapi==0.111.0
uvicorn[standard]==0.29.0
python-multipart==0.0.9

# Database — ORM + Drivers
sqlalchemy==2.0.30
alembic==1.13.1
asyncpg==0.29.0
psycopg2-binary==2.9.9

# Authentication
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.1

# Background Jobs
apscheduler==3.10.4

# LangChain + LLM (unpinned minor to resolve dependency tree)
langchain-core>=0.2.27,<0.4
langchain-groq>=0.2.0,<1.0
langchain-ollama>=0.2.0,<1.0
langchain-community>=0.3.0,<1.0
langchain-chroma>=0.1.4,<1.0
langgraph>=0.2.0,<1.0

# Embeddings + Vector Store
sentence-transformers>=2.7.0,<3.0
chromadb>=0.5.3,<1.0

# Email
resend>=2.0.0,<3.0

# Data Generation (for mock data seeding)
faker>=24.0.0,<30.0

# Validation + Configuration
pydantic>=2.7.0,<3.0
python-dotenv>=1.0.1,<2.0

# Testing (dev dependencies)
pytest>=8.0.0
pytest-asyncio>=0.23.0
httpx>=0.27.0

# Required by Pydantic EmailStr
email-validator>=2.0.0
